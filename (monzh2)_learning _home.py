from tabnanny import process_tokens
from tkinter.font import names  #Урок A-1: Волшебный мир Объектов. Создаём своих первых роботов!

# Представь, что ты волшебник, который может создавать любые предметы. Допустим, ты хочешь создать армию роботов.
# У каждого робота есть:
# Имя (например, "R2-D2" или "Валл-И").
# Цвет (серебристый, синий).
# Уровень заряда (в процентах).
# Умения (умеет летать, умеет говорить, умеет убирать).
# В программировании такой "чертёж" или "рецепт" для создания робота называется Класс.
# Класс — это просто чертёж, инструкция. Сам по себе он ничего не делает, как рецепт пирога на бумаге.
# Объект (или Экземпляр) — это уже настоящий робот, которого ты создал по этому чертежу.
# Как настоящий, только что испечённый пирог по рецепту.
# Атрибуты — это характеристики объекта (имя, цвет, заряд). Как "паспорт" робота.
# Методы — это действия, которые объект может выполнять (летать, говорить). Как "команды" для робота.
# Пример в коде (Python):
# Давай создадим класс для нашего робота.
#
# # Это наш чертёж (Класс) под названием "Robot"
# class Robot:
#     # Это специальный метод-конструктор. Он вызывается, когда мы "рождаем" нового робота.
#     # 'self' - это волшебное слово, которое означает "сам объект" (сам робот).
#     def __init__(self, name, color, battery=100):
#         # Создаём атрибуты (характеристики) для нашего робота.
#         self.name = name
#         self.color = color
#         self.battery = battery
#
#     # Это метод (действие) "представиться"
#     def introduce(self):
#         print(f"Привет! Я робот по имени {self.name}. Мой цвет {self.color}. Заряд: {self.battery}%.")
#
#     # Это метод (действие) "летать"
#     def fly(self):
#         if self.battery >= 10:
#             print(f"{self.name}: Вжух! Я лечу!")
#             self.battery -= 10 # Тратим заряд на полёт
#             print(f"Осталось заряда: {self.battery}%")
#         else:
#             print(f"{self.name}: Батарея разряжена! Не могу летать.")
#
#     # Метод "подзарядиться"
#     def charge(self):
#         self.battery = 100
#         print(f"{self.name}: Заряжен на 100%!")
# Теперь давай создадим двух настоящих роботов (объектов) по нашему чертежу!
#
# # Создаём первого робота
# robot1 = Robot("Валл-И", "жёлтый", 50)
# # Создаём второго робота
# robot2 = Robot("Ева", "белый")
#
# # Дадим команды нашим роботам
# robot1.introduce() # Выведет: Привет! Я робот по имени Валл-И. Мой цвет жёлтый. Заряд: 50%.
# robot2.introduce() # Выведет: Привет! Я робот по имени Ева. Мой цвет белый. Заряд: 100%.
# robot1.fly() # Валл-И полетит и потратит 10% заряда
# robot2.fly() # Ева полетит и потратит 10% заряда
#
# robot1.charge() # Валл-И зарядится
# Наследование: Создаём особенного робота-воина!
# Теперь представим, что нам нужен не просто робот, а Робот-Воин. У него есть всё то же самое, что и у обычного робота, но ещё есть оружие и он умеет атаковать.
# Зачем заново писать весь чертёж? Мы можем его унаследовать! Это как сказать: "Создай нового робота, который будет таким же, как обычный робот, но добавь ему вот эти новые фишки".
#
# # Класс RobotWarrior наследуется от класса Robot
# class RobotWarrior(Robot): # В скобках указываем, от кого наследуемся
#     def __init__(self, name, color, weapon, battery=100):
#         # super() - это обращение к родительскому классу (Robot).
#         # Мы вызываем конструктор родителя, чтобы задать name, color и battery.
#         super().__init__(name, color, battery)
#         # Добавляем НОВЫЙ атрибут, которого не было у родителя.
#         self.weapon = weapon
#
#     # Добавляем НОВЫЙ метод.
#     def attack(self):
#         if self.battery >= 20:
#             print(f"{self.name} атакует с помощью {self.weapon}!")
#             self.battery -= 20
#         else:
#             print(f"{self.name}: Недостаточно энергии для атаки!")
#
# # Создаём робота-воина
# warrior = RobotWarrior("Терминатор", "чёрный", "лазерная пушка")
# warrior.introduce() # Этот метод он унаследовал от Robot!
# warrior.attack()    # А этот метод его собственный!
# Что произошло?
# RobotWarrior — это дочерний класс (ребёнок).
# Robot — это родительский класс (родитель).
# Дочерний класс получает (наследует) все атрибуты и методы родителя.
# Дочерний класс может добавлять свои собственные атрибуты и методы.



                           #⭐ УРОК 1. ООП Часть 1 ⭐

      #A-2. Основы ООП, классы, объекты, атрибуты, методы, __init__, принцип Наследования

# Я объясняю просто, как ребёнку, но подробно как сильному программисту.
#  Что такое ООП?
# Представь, что программирование = создание “вещей”.
# ООП — это способ создавать эти “вещи” в твоём коде.
# Вещи = объекты
# Шаблон, чертёж вещи = класс
# Например:
# 🛠 Класс = «План» телефона
# 📱 Объект = настоящий телефон в твоей руке
# (и можно сделать много одинаковых телефонов)




                       # 🔥 C-1. Что такое КЛАСС?
# Класс — это чертёж.
# Каждый класс описывает:
#
# какие у объекта будут данные (атрибуты)
#
# что объект умеет делать (методы)
#
# Пример самого простого класса:
#
# class Person:
#     pass
#
# Пока он пустой, но это уже класс.




                       # 🔥 C-2. Что такое ОБЪЕКТ?
#
# Объект — это экземпляр класса, реальный экземпляр.
#
# p1 = Person()
# p2 = Person()
#
# У тебя есть 1 форма для печенья (класс)
# и ты делаешь 100 печений (объектов).





                         # 🔥 D-1. Что такое __init__ — конструктор
# Когда объект создаётся — запускается __init__.
# Он позволяет “дать характеристики” объекту.
#
# Пример:
# class Person:
#     def __init__(self, name, age):
#         self.name = name    # атрибут
#         self.age = age      # атрибут
#
# Теперь можно создавать людей:
#
# p1 = Person("Файиздин", 20)
# p2 = Person("Абдухалил", 25)




                        #🔥 D-2. Что такое АТРИБУТЫ?
# Атрибуты — это переменные внутри объекта.

# Пример:
# class Car:
#     def __init__(self, brand, year):
#         self.brand = brand
#         self.year = year
#
# Создаём объект:
# c = Car("BMW", 2020)
# print(c.brand)     # BMW
# print(c.year)      # 2020
#





                    # 🔥 E-1. Что такое МЕТОДЫ?
# Метод — это функция внутри класса.
# class Car:
#     def start(self):
#         print("Машина заводится...")
#
# Чтобы вызвать метод:
# car = Car()
# car.start()






                        #🔥 E-2. Пример класса целиком
# class Car:
#     def __init__(self, brand, year):
#         self.brand = brand
#         self.year = year
#
#     def start(self):
#         print(f"{self.brand} заводится...")








                   #🔥 F-1. Наследование — один из важнейших принципов ООП
# Оно означает:
# ➡️ Можно создать класс
# ➡️ И другой класс может взять от него все свойства
# Например:
# Есть класс Animal
# От него наследуются Cat и Dog
#
# class Animal:
#     def sound(self):
#         print("Какой-то звук")
#
# class Cat(Animal):
#     def sound(self):
#         print("Мяу")
#
# class Dog(Animal):
#     def sound(self):
#         print("Гав")
#
# Cat и Dog — “дети” Animal.
# Почему это удобно?
# Потому что:
# можно легко расширять
# можно переопределять методы
# меньше писать код
#
# 🔥 Большой пример Уровень: Джуниор Developer
# class Human:
#     def __init__(self, name, job):
#         self.name = name
#         self.job = job
#
#     def info(self):
#         print(f"Имя: {self.name}, профессия: {self.job}")
#
# class Programmer(Human):
#     def code(self):
#         print(f"{self.name} пишет код...")
#
# class Doctor(Human):
#     def heal(self):
#         print(f"{self.name} лечит людей...")
#
# Использование:
#
# p = Programmer("Файиздин", "Backend")
# d = Doctor("Абдухалил", "Хирург")
#
# p.info()   # Имя: Файиздин, профессия: Backend
# p.code()   # Файиздин пишет код
#
# d.info()
# d.heal()




                                        #---HOMEWORK---#

                                  #HOMEWORK A-1:
# class Car:
#      def __init__(self, model, year):
#       self.model = model
#       self.year = year
#
#
#
#      def start(self):
#         print('Car Start: ')
#
# car_mersedes = Car(model='Mercedes', year=2020)
# car_bmw = Car(model='BMW', year=2020)
# print(car_mersedes)
# print(car_mersedes.model, car_mersedes.year)
# car_mersedes.start()
# print(car_bmw)
# print(car_bmw.model, car_bmw.year)
# car_bmw.start()
#
#
#
#
#                                 #HOMEWORK A-2:
# class ElitaCar(Car):
#     def charge(self):
#         print('Машина зарежяется')
#
# supra = ElitaCar(model='Supra', year=2018)
# supra.charge()
# print(supra)



                                    #HOMEWORK B-1:
# class Robot:
#     def __init__(self, name, color, battery):
#         self.name = name
#         self.color = color
#         self.battery = battery
#
# robot1 = Robot(name="Gpt", color="white", battery=90)
# print("Robot1:", robot1.name, robot1.color, robot1.battery)
# robot2 = Robot(name="Deepsek", color="blue", battery=85)
# print("Robot2:", robot2.name, robot2.color, robot2.battery)
# robot3 = Robot(name="Gemini", color="black", battery=80)
# print("Robot3:", robot3.name, robot3.color, robot3.battery)




                                    #HOMEWORK B-2:
# class Cat:
#     def __init__(self, name, age, color):
#         self.name = name
#         self.age = age
#         self.color = color
#
#     def meow(self):
#         print(f'meeoow {self.name}')
#
#     def scratch(self):
#         print(f'scratch {self.name} 4-5 day.')
#
#     def sleep(self):
#         print(f'sleep {self.name} 9 night.')
#
#
# Misha = Cat('Misha', 4, 'blue')
# print("Cat:", Misha.name, Misha.age, Misha.color)
# Misha.meow()
# Misha.scratch()
# Misha.sleep()



                                #HOMEWORK B-3:
# class RobotCleaner:
#     def __init__(self, name, color, trash_bog_size):
#         self.name = name
#         self.color = color
#         self.trash_bog_size = trash_bog_size
#
#     def clean_room(self):
#         print(f'{self.name} cleaned')
#
#
# Claud = RobotCleaner("Claud", "white", 90)
# print("RobotCleaner:", Claud.name, Claud.color, Claud.trash_bog_size)
# Claud.clean_room()






#----------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------



               #⭐ УРОК 2 — ООП Часть 2 ⭐ Инкапсуляция, Полиморфизм
                    # Урок 2: Секреты, Перевоплощения и Умные Переключатели
# Объясняю, как для ребёнка:
# В прошлый раз мы научились создавать роботов.
# Но что, если наш робот хранит секретные данные?
# Или если у нас есть волшебный пульт, которым можно управлять и телевизором, и роботом, и стиральной машиной одной кнопкой?
# Сегодня мы изучим три суперсилы программирования!


                    #A-1: Инкапсуляция (Сокрытие данных или "Секретный сейф")
# Представь, что у твоего робота есть секретная заводская настройка — серийный номер.
# Его нельзя менять просто так, и не каждый должен его видеть.
# Мы спрячем его в "сейф" внутри робота.
#
# Public (Публичный) — как имя робота. Все могут его видеть и менять.
# Protected (Защищённый) — с одной чёрточкой _.
# Как папка с надписью "Не трогать, но если очень надо — можно". Это намёк для программистов.
# Private (Приватный) — с двумя чёрточками __.
# Настоящий сейф! К нему нельзя обратиться напрямую снаружи класса.

# Пример:
# class SecureRobot:
#     def __init__(self, name, secret_number):
#         self.name = name                 # Публичный атрибут (все видят)
#         self._password = "12345"         # Защищённый (условно приватный)
#         self.__serial_number = secret_number # Приватный (настоящий сейф!)
#
#     def tell_secret(self):
#         # Правильно: доступ к приватному атрибуту ВНУТРИ класса
#         print(f"Мой секретный номер... шепотом: {self.__serial_number}")
#
#     # Геттер (get - получить) - метод чтобы ПОСМОТРЕТЬ приватный атрибут
#     def get_serial(self):
#         return self.__serial_number
#
#     # Сеттер (set - установить) - метод чтобы ИЗМЕНИТЬ приватный атрибут с проверкой
#     def set_serial(self, new_number):
#         if len(str(new_number)) == 10: # Проверяем, что номер из 10 цифр
#             self.__serial_number = new_number
#             print("Номер успешно изменён!")
#         else:
#             print("Ошибка! Серийный номер должен быть из 10 цифр.")
#
# # Создаём робота
# bot = SecureRobot("Секретчик", 9876543210)
#
# print(bot.name)          # Работает: "Секретчик"
# print(bot._password)     # Работает, но так делать нежелательно: "12345"
# # print(bot.__serial_number) # ОШИБКА! Нельзя обратиться напрямую!
#
# bot.tell_secret() # Работает: робот сам говорит свой номер
#
# # Используем геттер и сеттер
# print(f"Узнаю номер через геттер: {bot.get_serial()}") # Узнали
# bot.set_serial(1234567890) # Успешно изменили
# bot.set_serial(123)        # Ошибка! Не прошло проверку
#
# Зачем это нужно?
# Чтобы защитить важные данные от случайного изменения и контролировать, как с ними взаимодействуют.




                #🔥A-2. Инкапсуляция — простое объяснение
# Инкапсуляция = спрятать важные данные, чтобы их никто не сломал.
# Например:
# в банке нельзя напрямую менять баланс
# в машине нельзя напрямую крутить параметры двигателя
# во многих программах есть “доступ только через методы”
# В Python мы прячем данные, добавляя:
# _protected — защищённый (рекомендация “не трогай руками”)
# __private — полностью скрытый (реально спрятан)
#
# 💡 Пример 1: скрываем приватный баланс банка
# class BankAccount:
#     def __init__(self, balance):
#         self.__balance = balance   # приватная переменная
#
#     def deposit(self, amount):
#         self.__balance += amount
#
#     def get_balance(self):
#         return self.__balance
#
# Попробуй:
# acc = BankAccount(1000)
# acc.deposit(500)
# print(acc.get_balance())   # 1500
#
# Если ты попытаешься сделать:
# acc.__balance = 999999
#
# Это НЕ изменит приватный баланс, так как он скрыт.
# 💡 Пример 2: защищённые атрибуты
# class User:
#     def __init__(self, name):
#         self._name = name  # защищённый
#
# Это как знак:
# “Можно использовать, но аккуратно!”
# 🔥 Почему инкапсуляция важна?
# Потому что:
# защищает данные
# предотвращает ошибки
# делает код профессиональным
# делает проект безопаснее
# Все реальные приложения используют инкапсуляцию.





            #B-1: Полиморфизм (Одна кнопка — много действий или "Волшебный пульт")
# Слово "полиморфизм" звучит сложно, но смысл прост: "Одна форма — много действий".
# У тебя есть пульт с кнопкой "Включить".
# Нажал на телевизоре — телевизор включился.
# Нажал на роботе — робот проснулся.
# Нажал на машине — машина завелась.
# Кнопка одна, а результат разный! В программировании это значит,
# что одинаковые имена методов могут делать разное для разных объектов.
#
# Пример 1: Разные классы, один метод
# class TV:
#     def turn_on(self):
#         print("Телевизор: показываю мультики!")
#
# class Robot:
#     def turn_on(self):
#         print("Робот: Здравствуйте, я готов к работе!")
#
# class Car:
#     def turn_on(self):
#         print("Машина: Врум-врум! Завелась!")
#
# # Создаём объекты
# my_tv = TV()
# my_robot = Robot()
# my_car = Car()
#
# # Волшебный пульт (один и тот же метод для всех)
# for device in [my_tv, my_robot, my_car]:
#     device.turn_on() # Одна команда, разные результаты!
#
# Пример 2: Полиморфизм в наследовании (Переопределение методов)
# Робот-уборщик при полёте может подметать!
#
# class Robot:
#     def action(self):
#         print("Робот: Я просто двигаюсь.")
#
# class RobotCleaner(Robot):
#     # Переопределяем метод родителя!
#     def action(self):
#         print("Робот-уборщик: Летаю и подметаю пол! Вжух-ш-ш-ш")
#
# class RobotWarrior(Robot):
#     # Тоже переопределяем!
#     def action(self):
#         print("Робот-воин: Летаю и ищу цель! Трах-бабах!")
#
# # Создаём список разных роботов
# robots = [Robot(), RobotCleaner(), RobotWarrior()]
#
# for r in robots:
#     r.action() # Вызовется СВОЙ метод для каждого типа робота!





                        #🔥 2. Полиморфизм — простое объяснение
# Полиморфизм = одно и то же действие → разный результат
# Примеры из жизни:
# ты говоришь “Ешь” → кошка ест по-кошачьи, собака по-собачьи
# “Пой” → птицы поют по-разному
# “Запусти” → разные машины запускаются по-разному
#
# 💡 Пример 1: разные животные дают разные звуки
# class Animal:
#     def sound(self):
#         pass
#
# class Cat(Animal):
#     def sound(self):
#         print("Мяу")
#
# class Dog(Animal):
#     def sound(self):
#         print("Гав")
#
# class Cow(Animal):
#     def sound(self):
#         print("Мууу")
#
# Использование:
#
# animals = [Cat(), Dog(), Cow()]
# for a in animals:
#     a.sound()
#
# Результат:
# Мяу
# Гав
# Мууу
#
# Один метод → три разных результата.
#
# 💡 Пример 2: полиморфизм в реальной жизни
# class Payment:
#     def pay(self):
#         pass
#
# class CardPayment(Payment):
#     def pay(self):
#         print("Оплата картой")
#
# class CashPayment(Payment):
#     def pay(self):
#         print("Оплата наличными")
#
# class CryptoPayment(Payment):
#     def pay(self):
#         print("Оплата криптовалютой")




                        #🔥C-1. Полиморфизм + Инкапсуляция вместе
# Очень частая комбинация:
# class Phone:
#     def __init__(self, model):
#         self.__model = model  # скрытый атрибут
#
#     def get_model(self):
#         return self.__model
#
# class Samsung(Phone):
#     def ring(self):
#         print("Samsung звонит - тин дин")
#
# class IPhone(Phone):
#     def ring(self):
#         print("iPhone звонит - тын тын")
#


                         #🔥C-2. Немного более сложный реальный пример
# 🎯 Класс-родитель: Worker
# 🎯 Дети: Programmer и Designer
# class Worker:
#     def __init__(self, name, salary):
#         self.__salary = salary  # скрыто
#
#         self.name = name
#
#     def get_salary(self):
#         return self.__salary
#
#     def work(self):
#         pass
#
# Дочерние классы:
# class Programmer(Worker):
#     def work(self):
#         print(f"{self.name} пишет код")
#
#
# class Designer(Worker):
#     def work(self):
#         print(f"{self.name} рисует дизайн")
#
# Использование:
# workers = [
#     Programmer("Файиздин", 500),
#     Designer("Абдухалил", 450)
# ]
#
# for w in workers:
#     w.work()



                                # 🎯 УРОВЕНЬ: Middle Developer
                                # Полиморфизм используется:
                                # в фреймворках
                                # в Django
                                # в Flask
                                # при работе с API
                                # в обработке событий
                                # в написании моделей данных
                                # Это профессиональный навык.



                                    #---HOMEWORK---#

                                   #HOMEWRK A-1:
# class SecretAgent:
#     def __init__(self, name, real_name, secret_name):
#         self.name = name
#         self.real_name = real_name
#         self.secret_name = secret_name
#         self.__mission_count = 0
#
#
#
#     def complete_mission(self):
#         self.__mission_count += 1
#         print('Alinur')
#
#     def get_mission_count(self):
#         return self.__mission_count
#
#     def set_real_name(self, new_name):
#         if "Alinur" in new_name.strip():
#             self.real_name = new_name
#             print("Настоящее имя успешно изменено.")
#         else:
#             print("Ошибка: введите имя и фамилию через пробел.")
#
#
#
# agent = SecretAgent("Alinur", "Robert", "Asik")
# print("Agent007:", agent.name, agent.real_name, agent.secret_name)
#
# agent.complete_mission()
# agent.get_mission_count()
# agent.set_real_name(agent.real_name)






                                    #HOMEWRK A-2:
# class Animal:
#     def __init__(self, __energy):
#         self.__energy = 100
#
#     def get_energy(self):
#         return self.__energy
#
#
#     def sound(self):
#         print('MYAW')
#
#
# Cat = Animal('')
# print(Cat.get_energy())
# Cat.sound()



                                        #HOMEWRK A-3:
# class Animal:
#     def __init__(self, name):
#         self.name = name
#
# class Cat(Animal):
#     def sound(self):
#         print("MYAW")
#

# class Dog(Animal):
#     def sound(self):
#         print("WOW")
#
#
# class Cow(Animal):
#     def sound(self):
#         print("MUU")
#
# Elza = Cat("")
# Elza.sound()
#
# Reks = Dog("")
# Reks.sound()
#
# Marusya = Cow("")
# Marusya.sound()
#
# animals = [Cat(''), Dog(''), Cow('')]
#
# for animal in animals:
#     animal.sound()




                                        #HOMEWRK A-4:
# class Calculator:
#     def calculate(self, a, b):
#         return 0
#
# class Adder(Calculator):
#     def calculate(self, a, b):
#         return a + b
#
#
# class Multiplier(Calculator):
#     def calculate(self, a, b):
#         return a * b
#
# class Subtracter(Calculator):
#     def calculate(self, a, b):
#         return a - b
#
#
#
# calculators = [Adder(), Multiplier(), Subtracter()]
#
#
# for calc in calculators:
#     print(calc.calculate(10, 5))




                                        #Homework A-5:
# class BankAccount:
#     def __init__(self):
#         self.__balance = 0      # приватный атрибут
#
#     def deposit(self, amount):
#         self.__balance += amount
#         print(f"Пополнение: +{amount}")
#
#     def withdraw(self, amount):
#         if amount <= self.__balance:
#             self.__balance -= amount
#             print(f"Снятие: -{amount}")
#         else:
#             print("❌ Недостаточно средств!")
#
#     def get_balance(self):
#         return self.__balance
#
# # Проверка
# account = BankAccount()
#
# account.deposit(1000)
# print("Баланс:", account.get_balance())
#
# account.withdraw(500)
# print("Баланс:", account.get_balance())
#
# account.withdraw(700)
# print("Баланс:", account.get_balance())






#--------------------------------------------------------------------------------------------
#--------------------------------------------------------------------------------------------





      #⭐ УРОК 3 — Магические, статические, классовые методы и множественное наследование ⭐
                     # Урок 3: Магия, Статика и Множественные Родители

# Объясняю, как для ребёнка:
# Сегодня мы узнаем о магических методах,
# которые позволяют твоим объектам вести себя как встроенные типы Python
# (например, сравниваться, складываться, выводиться на печать).
# Затем научимся создавать методы,
# которые работают без создания объекта — статические и классовые.
# И наконец, разберёмся, что делать,
# если нашему роботу нужно унаследовать умения сразу от двух других роботов.


                         #A - 1: Магические методы (Dunder Methods)

# Магические методы начинаются и заканчиваются двумя подчёркиваниями: __метод__.
# Они вызываются автоматически в определённых ситуациях. Это как волшебные слова для объектов.
#
# Примеры:
# class MagicalRobot:
#     def __init__(self, name, power):
#         self.name = name
#         self.power = power
#
#     # 1. __str__: вызывается при печати объекта print() или str()
#     def __str__(self):
#         return f"Магический робот {self.name} (сила: {self.power})"
#
#     # 2. __repr__: вызывается при выводе в консоли (более техническое представление)
#     def __repr__(self):
#         return f"MagicalRobot('{self.name}', {self.power})"
#
#     # 3. __eq__: вызывается при сравнении ==
#     def __eq__(self, other):
#         return self.power == other.power
#
#     # 4. __lt__: вызывается при сравнении < (less than)
#     def __lt__(self, other):
#         return self.power < other.power
#
#     # 5. __add__: вызывается при сложении +
#     def __add__(self, other):
#         # Создаём нового робота при сложении
#         new_name = f"{self.name}+{other.name}"
#         new_power = self.power + other.power
#         return MagicalRobot(new_name, new_power)
#
# # Создаём роботов
# robot1 = MagicalRobot("Титан", 100)
# robot2 = MagicalRobot("Голиаф", 150)
#
# print(robot1)           # Автоматически вызывает __str__: "Магический робот Титан (сила: 100)"
# print(repr(robot1))     # Автоматически вызывает __repr__: "MagicalRobot('Титан', 100)"
#
# print(robot1 == robot2) # False (100 != 150) - вызывает __eq__
# print(robot1 < robot2)  # True (100 < 150) - вызывает __lt__
#
# # Теперь самое интересное - СЛОЖЕНИЕ роботов!
# robot3 = robot1 + robot2  # Вызывает __add__
# print(robot3)             # Магический робот Титан+Голиаф (сила: 250)



                        #🔥A - 2. Магические методы — что это вообще такое?

# Магические методы — это специальные методы с двойными подчёркиваниями:
#
# __init__
# __str__
# __len__
# __add__
#
# Они автоматически вызываются Python’ом, а не тобой вручную.
# ✅ Самый главный магический метод — __init__
# Ты уже его знаешь — он вызывается при создании объекта:
#
# class Person:
#     def __init__(self, name):
#         self.name = name
#
# p = Person("Файиздин")   # тут автоматически вызывается __init__
#
#
# ✅ Магический метод __str__ — как объект показывается как строка
# Без __str__:
#
# class User:
#     def __init__(self, name):
#         self.name = name
#
# u = User("Файиздин")
# print(u)
#
# Выведет что-то типа:
#
# <__main__.User object at 0x000001>
#
# ❌ Ничего не понятно.
#
# ✅ С __str__:
#
# class User:
#     def __init__(self, name):
#         self.name = name
#
#     def __str__(self):
#         return f"Пользователь: {self.name}"
#
# u = User("Файиздин")
# print(u)
#
# ✅ Вывод:
# Пользователь: Файиздин
#
# ✅ Магический метод __len__
# Позволяет использовать len() на объекте:
#
# class Basket:
#     def __init__(self, items):
#         self.items = items
#
#     def __len__(self):
#         return len(self.items)
#
# b = Basket([1, 2, 3, 4])
# print(len(b))   # 4
#
# 🔥 Вывод по магическим методам:
# Они позволяют:
# красиво выводить объект
# управлять len()
# управлять сложением объектов
# управлять сравнением
# делать объект как число, как строку и т.д.



                        #B - 1: Статические и классовые методы
# Иногда нам нужны методы, которые:
# Не требуют создания объекта (работают на уровне класса)
# Не имеют доступа к self (к конкретному объекту)
#
# class RobotFactory:
#     # Атрибут класса - общий для ВСЕХ объектов этого класса
#     total_robots_created = 0
#     factory_name = "Галактическая фабрика роботов"
#
#     def __init__(self, name):
#         self.name = name
#         RobotFactory.total_robots_created += 1
#
#     # Обычный метод - работает с КОНКРЕТНЫМ объектом (self)
#     def introduce(self):
#         print(f"Я робот {self.name}")
#
#     # Классовый метод - работает с КЛАССОМ в целом (cls)
#     # Может обращаться к атрибутам класса
#     @classmethod
#     def get_factory_info(cls):
#         print(f"Фабрика: {cls.factory_name}")
#         print(f"Всего создано роботов: {cls.total_robots_created}")
#
#     # Статический метод - не получает ни self, ни cls
#     # Просто функция, "живущая" внутри класса для удобства
#     @staticmethod
#     def calculate_power(weight, speed):
#         # Не зависит ни от объекта, ни от класса
#         return weight * speed
#
# # Используем КЛАССОВЫЙ метод без создания объектов!
# RobotFactory.get_factory_info()
#
# # Создаём роботов
# r1 = RobotFactory("Робот-1")
# r2 = RobotFactory("Робот-2")
#
# # Снова смотрим информацию - теперь создано 2 робота
# RobotFactory.get_factory_info()
#
# # Используем СТАТИЧЕСКИЙ метод
# power = RobotFactory.calculate_power(weight=100, speed=10)
# print(f"Мощность: {power}")  # 1000
#
# # Можем вызывать статический метод и из объекта
# print(r1.calculate_power(50, 20))  # 1000




                           #🔥B - 2. Статические методы (@staticmethod)

# Статический метод — это метод, который:
# НЕ использует self
# НЕ использует cls
# просто логически относится к классу
#
# ✅ Пример очень простой:
# class Math:
#     @staticmethod
#     def add(a, b):
#         return a + b
#
# Вызов:
#
# print(Math.add(5, 3))   # 8
#
# 👉 Ты не создавал объект, а метод всё равно работает.
#
# ✅ Реальный пример:
# class User:
#     @staticmethod
#     def is_adult(age):
#         return age >= 18
#
# print(User.is_adult(20))   # True
# print(User.is_adult(15))   # False


             #C - 1: Множественное наследование(Робот - трансформер)
# Что если нашему роботу нужно уметь  и летать(как робот - лётчик), и плавать(как робот - подводник)?
# Можно наследоваться от двух классов сразу!
#
# class FlyingRobot:
#     def __init__(self, max_altitude):
#         self.max_altitude = max_altitude
#
#                  def fly(self):
#                      print(f"Лечу на высоте {self.max_altitude} метров!")
#
#
#              class SwimmingRobot:
#                  def __init__(self, max_depth):
#                      self.max_depth = max_depth
#
#                  def swim(self):
#                      print(f"Плыву на глубине {self.max_depth} метров!")
#
#
#              # Робот-трансформер наследует от ДВУХ родителей
#              class TransformerRobot(FlyingRobot, SwimmingRobot):
#                  def __init__(self, name, max_altitude, max_depth):
#                      self.name = name
#                      # Вызываем конструкторы родителей
#                      FlyingRobot.__init__(self, max_altitude)
#                      SwimmingRobot.__init__(self, max_depth)
#
#                  def transform_and_act(self):
#                      print(f"{self.name} трансформируется!")
#                      self.fly()  # Метод от FlyingRobot
#                      self.swim()  # Метод от SwimmingRobot
#
#
#              # Создаём трансформера
#       optimus_prime = TransformerRobot("Оптимус Прайм", 1000, 500)
#     optimus_prime.transform_and_act()
#              # Оптимус Прайм трансформируется!
#              # Лечу на высоте 1000 метров!
#              # Плыву на глубине 500 метров!
#
#              # Важно: порядок наследования имеет значение!
#              print(TransformerRobot.__mro__)  # Показывает порядок поиска методов
# Проблема "ромбовидного наследования" и её решение(MRO - Method Resolution Order):
# Python ищет методы в определённом порядке(слева направо в скобках наследования, затем вверх поиерархии).
# Это предотвращает неоднозначност




                            #🔥C - 1. Классовые методы (@classmethod)

# Классовый метод:
# работает с самим классом
# принимает параметр cls
# часто используется для счётчиков объектов
#
# ✅ Пример: считаем пользователей
# class User:
#     count = 0   # атрибут класса
#
#     def __init__(self, name):
#         self.name = name
#         User.count += 1
#
#     @classmethod
#     def get_count(cls):
#         return cls.count
#
#
# Использование:
#
# u1 = User("Файиздин")
# u2 = User("Абдухалил")
# print(User.get_count())  # 2
#
# ✅ Отличие:
# Тип метода	Использует
# Обычный	self
# Статический	ничего
# Классовый	cls





                            #🔥D - 1. Множественное наследование

# Это когда класс наследуется от двух и более классов.
#
# ✅ Пример:
# class Flyer:
#     def fly(self):
#         print("Летает")
#
# class Swimmer:
#     def swim(self):
#         print("Плавает")
#
# class Duck(Flyer, Swimmer):
#     pass
#
# Использование:
#
# d = Duck()
# d.fly()    # Летает
# d.swim()   # Плавает
#
# ✅ Один класс — получил возможности от двух классов.
# ✅ Реальный пример:
# class Camera:
#     def take_photo(self):
#         print("Фотка сделана")
#
# class Phone:
#     def call(self):
#         print("Совершается звонок")
#
# class SmartPhone(Camera, Phone):
#     pass
#
#
#
#
#                     #🔥E - 1. Большой реальный пример (почти как в проектах)
#
# class User:
#     count = 0
#
#     def __init__(self, name):
#         self.name = name
#         User.count += 1
#
#     def __str__(self):
#         return f"Пользователь: {self.name}"
#
#     @staticmethod
#     def is_valid_name(name):
#         return len(name) >= 3
#
#     @classmethod
#     def get_total_users(cls):
#         return cls.count
#
#
# Использование:
#
# print(User.is_valid_name("Фай"))  # True
# u1 = User("Файиздин")
# u2 = User("Абдухалил")
# print(u1)
# print(User.get_total_users())  # 2






                                            #Homework A-1:
# class MathRobot:
#     def __init__(self, name, power):
#         self.name = name
#         self.power = power
#
#     def __add__(self, other):
#         return  self.power + other.power
#
#     def __sub__(self, other):
#         return self.power - other.power
#
#     def __mul__(self, other):
#         return self.power * other.power
#
#     def __truediv__(self, other):
#         return self.power / other.power
#
#     def __len__(self):
#         return len(self.name)
#
#
# Robot1 = MathRobot('Alinur',100)
# Robot2 = MathRobot('Alina', 25)
#
# print(Robot1 + Robot2)
# print(Robot1 - Robot2)
# print(Robot1 * Robot2)
# print(Robot1 / Robot2)
# print(len(Robot1))




                                        #Homework A-2:
# class User:
#     count = 0
#     def __init__(self, name, email):
#         self.name = name
#         self.email = email
#         User.count += 1
#
#
#     def __str__(self):
#         return f'Имя: {self.name}, Email: {self.email}.'
#
#     @staticmethod
#     def is_email_valid(email):
#         return '@' in email
#
#
#     @classmethod
#     def get_count(cls):
#         return cls.count
#
#
#
# class Boss(User):
#     count = 0
#     def __init__(self, name):
#         super().__init__(name, "weenox62@gmail.com")
#
#
# class Moderator(User):
#     count = 0
#     def __init__(self, name):
#         super().__init__(name, "weenzox62gmail.com")
#
#
# Admin1 = User('Faizdin', 'faizaltibaev@gmail.com')
# Admin2 = User('Alinur', 'Aliabdulaev@gmail.com')
# print(Admin1)
# print(Admin2)
#
#
# print(Admin1.is_email_valid('fazaltibaev21@gmail.com'))
# print(Admin1.is_email_valid('faizaltibaev21gmail.com'))
# print(Admin1.get_count())
# print(Admin2.get_count())
#
#
# Boss1 = Boss('Sasha')
# print(Boss1)
# print(Boss1.is_email_valid('weenoxgmailgmail.com'))
# print(Boss1.get_count())
#
# Mod1 = Moderator('Misha')
# print(Mod1)
# print(Mod1.is_email_valid('weenzox@gmailgmail.com'))
# print(Mod1.get_count())

                                #Homework B-1:
# import random
#
# class RobotFactory:
#     robot_count = 0
#     robot_list = []   # чтобы хранить созданных роботов
#
#     def __init__(self, name):
#         self.name = name
#         RobotFactory.robot_count += 1
#         RobotFactory.robot_list.append(self)
#
#     @classmethod
#     def create_random_robot(cls):
#         names = ["Alpha", "Beta", "Orion", "Xenon", "R-77", "T900"]
#         name = random.choice(names)
#         return cls(name)
#
#     @staticmethod
#     def validate_name(name):
#         return name.isalnum()   # только буквы и цифры
#
#     def __str__(self):
#         return f"Робот: {self.name}"
#
# # --- Создание 3 случайных роботов ---
# r1 = RobotFactory.create_random_robot()
# r2 = RobotFactory.create_random_robot()
# r3 = RobotFactory.create_random_robot()
#
# print(r1)
# print(r2)
# print(r3)
#
# #--- Общая статистика ---
# print("Всего роботов создано:", RobotFactory.robot_count)
#
#
#


                                #Homework B-2:
#
# class Walker:
#     def __init__(self, legs):
#         self.legs = legs
#
#
#     def walk(self):
#         print(f'Я хожу на {self.legs} ногах')
#
#
#
# class Speaker:
#     def __init__(self, language):
#         self.language = language
#
#     def say_hello(self):
#         print(f'Я могу говорить на {self.language}')
#
#
#
# class Thinker:
#     def __init__(self, iq):
#         self.iq = iq
#
#     def solve_problem(self):
#         print(f'Мой iq {self.iq}')
#
#
#
# class SuperRobot(Walker, Speaker, Thinker):
#     def __init__(self, legs, language, iq):
#         super().__init__(self)
#         self.legs = legs
#         self.language = language
#         self.iq = iq
#
# robot = SuperRobot(legs=2, language='English', iq=195)
# robot.walk()
# robot.say_hello()
# robot.solve_problem()
#




                                    #Homework C-1:
# class A:
#     def do_action(self):
#         print("do action")
#
# class B:
#     def do_action(self):
#         print("do action")
#
# class C(A,B):
#     pass
#
# class D(B,A):
#     pass
#
# c = C()
# d = D()
# c.do_action()
# d.do_action()
#
# print("C MRO: ", C.__mro__)
# print("D MRO: ", D.__mro__)



                                    #Homework C-2:
# class BattleRobot:
#     def __init__(self, name, health, damage):
#         self.name = name
#         self.health = health
#         self.damage = damage
#
#     def __gt__(self, other):
#         if not  isinstance(other, BattleRobot):
#             return NotImplemented
#         return self.damage > other.damage
#
#     def __ge__(self, other):
#         if not isinstance(other, BattleRobot):
#             return NotImplemented
#         return self.health >= other.health
#
#     def __bool__(self):
#         return self.health > 0
#
#     def __str__(self):
#         return f'{self.name} (Health: {self.health}, Damage: {self.damage})'
#
#
#
# robot1 = BattleRobot('Robot1', 100, 85)
# robot2 = BattleRobot('Robot2', 90, 90)
# robot3 = BattleRobot('Robot3', 60, 100)
#
#
# print(f'{robot1.name} < {robot2.name}?', robot1 > robot2)
# print(f'{robot1.name} >= {robot2.name}?', robot1 >= robot2)
#
# robots = [robot1, robot2, robot3]
# for robot in robots:
#     print(f'{robot} is alive? {bool(robot)}')





#---------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------





            # ⭐ УРОК 4 — Использование ООП в проекте ⭐
            # Это практический урок. Ты научишься применять всё, что изучил на уроках 1–3.
            # Здесь мы создадим реальный мини-проект на Python, полностью на ООП.
            # Урок 4: Большой проект — Космическая станция управления роботами
            # Объясняю, как для ребёнка:

# Представь, что ты главный инженер на космической станции.
# У тебя есть разные типы роботов, база данных, система логирования и командный центр.
# Мы создадим полноценную систему управления, используя ВСЁ, что изучили!



                            #A-1: Архитектура проекта
# Мы создадим несколько модулей(файлов):
#
# robots.py — классы  роботов
#
# station.py — класс космической станции
#
# logger.py — система логирования
#
# main.py — главная  программа
#
# robots.py — Базовые классы  роботов python

# import random
# from datetime import datetime
#
# class BaseRobot:
#     """Базовый класс для всех роботов"""
#
#     total_robots = 0  # Атрибут класса
#
#     def __init__(self, name, model):
#         self.name = name
#         self.model = model
#         self._energy = 100  # Защищённый атрибут
#         self.__serial = self._generate_serial()  # Приватный
#         self.is_active = True
#         self.created_at = datetime.now()
#         BaseRobot.total_robots += 1
#
#     def _generate_serial(self):
#         """Защищённый метод генерации серийного номера"""
#         return f"SR-{random.randint(1000, 9999)}-{self.model}"
#
#     # Геттер для приватного атрибута
#     def get_serial(self):
#         return self.__serial
#
#     # Магические методы
#     def __str__(self):
#         return f"{self.name} ({self.model})"
#
#     def __repr__(self):
#         return f"BaseRobot('{self.name}', '{self.model}')"
#
#     def __eq__(self, other):
#         return self.model == other.model
#
#     def __add__(self, other):
#         """Объединение роботов в группу"""
#         return RobotGroup([self, other])
#
#     # Статический метод
#     @staticmethod
#     def validate_name(name):
#         return len(name) >= 2 and name.isalnum()
#
#     # Классовый метод
#     @classmethod
#     def get_total_count(cls):
#         return f"Всего создано роботов: {cls.total_robots}"
#
#
# robots.py — Специализированные роботы(наследование)
#
# class ResearchRobot(BaseRobot):
#     """Робот-исследователь"""
#
#     def __init__(self, name, model, lab_type):
#         super().__init__(name, model)
#         self.lab_type = lab_type
#         self._research_data = []
#
#     def conduct_research(self, topic):
#         if self._energy >= 20:
#             result = f"Исследование '{topic}' завершено"
#             self._research_data.append(result)
#             self._energy -= 20
#             return result
#         return "Недостаточно энергии!"
#
#     def get_research_count(self):
#         return len(self._research_data)
#
#     # Переопределяем магический метод
#     def __str__(self):
#         return f"Исследователь {self.name} ({self.lab_type} лаборатория)"
#
#
# class RepairRobot(BaseRobot):
#     """Робот-ремонтник"""
#
#     def __init__(self, name, model, tools):
#         super().__init__(name, model)
#         self.tools = tools
#         self.repairs_done = 0
#
#     def repair(self, target_robot):
#         if self._energy >= 30 and target_robot.is_active == False:
#             target_robot.is_active = True
#             self.repairs_done += 1
#             self._energy -= 30
#             return f"Отремонтировал {target_robot.name}"
#         return "Невозможно выполнить ремонт"
#
#     def __str__(self):
#         return f"Ремонтник {self.name} | Инструменты: {', '.join(self.tools)}"
#
#
# class SecurityRobot(BaseRobot):
#     """Робот-охранник"""
#
#     def __init__(self, name, model, weapon):
#         super().__init__(name, model)
#         self.weapon = weapon
#         self.threats_neutralized = 0
#
#     def patrol(self, sector):
#         if self._energy >= 15:
#             threats = random.choice([0, 0, 0, 1, 2])  # Случайные угрозы
#             self.threats_neutralized += threats
#             self._energy -= 15
#             return f"Патрулирование {sector}: нейтрализовано угроз: {threats}"
#         return "Недостаточно энергии для патруля"
#
#     # Магический метод для сравнения по эффективности
#     def __gt__(self, other):
#         return self.threats_neutralized > other.threats_neutralized
#
#
# robots.py — Множественное наследование и композиция
#
# class RobotGroup:
#     """Группа роботов (композиция)"""
#
#     def __init__(self, robots):
#         self.robots = robots
#
#     def __len__(self):
#         return len(self.robots)
#
#     def __getitem__(self, index):
#         return self.robots[index]
#
#     def total_energy(self):
#         return sum(r._energy for r in self.robots)
#
#     def activate_all(self):
#         for robot in self.robots:
#             robot.is_active = True
#
#
# # Робот с множественным наследованием
# class UniversalRobot(ResearchRobot, RepairRobot):
#     """Универсальный робот-исследователь-ремонтник"""
#
#     def __init__(self, name, model, lab_type, tools):
#         # Вызываем конструкторы обоих родителей
#         ResearchRobot.__init__(self, name, model, lab_type)
#         RepairRobot.__init__(self, name, model, tools)
#         self.mode = "исследователь"  # По умолчанию
#
#     def switch_mode(self, new_mode):
#         if new_mode in ["исследователь", "ремонтник"]:
#             self.mode = new_mode
#             return f"Режим изменён на: {new_mode}"
#         return "Неизвестный режим"
#
#     # Полиморфизм: метод действует в зависимости от режима
#     def perform_action(self, target=None):
#         if self.mode == "исследователь":
#             return self.conduct_research("автоматическое исследование")
#         elif self.mode == "ремонтник" and target:
#             return self.repair(target)
#         return "Нет цели для действия"
#
#
# logger.py — Система логирования
#
#
# class RobotLogger:
#     """Логгер для записи действий роботов"""
#
#     def __init__(self, filename="robot_log.txt"):
#         self.filename = filename
#         self._clear_log()  # Очищаем старый лог при создании
#
#     def _clear_log(self):
#         with open(self.filename, 'w', encoding='utf-8') as f:
#             f.write("=== Лог космической станции ===\n")
#
#     def log(self, message, robot=None):
#         timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
#         if robot:
#             log_entry = f"[{timestamp}] {robot}: {message}\n"
#         else:
#             log_entry = f"[{timestamp}] СИСТЕМА: {message}\n"
#
#         print(log_entry.strip())  # Выводим в консоль
#         with open(self.filename, 'a', encoding='utf-8') as f:
#             f.write(log_entry)  # Записываем в файл
#
#     @staticmethod
#     def read_log(filename="robot_log.txt"):
#         try:
#             with open(filename, 'r', encoding='utf-8') as f:
#                 return f.read()
#         except FileNotFoundError:
#             return "Лог-файл не найден"
#
#
# station.py — Космическая станция
#
#
# class SpaceStation:
#     """Главный управляющий класс космической станции"""
#
#     def __init__(self, name):
#         self.name = name
#         self.robots = []
#         self.logger = RobotLogger()
#         self.logger.log(f"Инициализация станции '{name}'")
#
#     def add_robot(self, robot):
#         if robot not in self.robots:
#             self.robots.append(robot)
#             self.logger.log(f"Добавлен робот: {robot}", robot)
#             return True
#         return False
#
#     def remove_robot(self, robot):
#         if robot in self.robots:
#             self.robots.remove(robot)
#             self.logger.log(f"Удалён робот: {robot}", robot)
#             return True
#         return False
#
#     def find_robot_by_name(self, name):
#         for robot in self.robots:
#             if robot.name == name:
#                 return robot
#         return None
#
#     def get_active_robots(self):
#         return [r for r in self.robots if r.is_active]
#
#     def get_inactive_robots(self):
#         return [r for r in self.robots if not r.is_active]
#
#     def group_by_type(self):
#         """Группировка роботов по типу (полиморфизм)"""
#         groups = {}
#         for robot in self.robots:
#             robot_type = type(robot).__name__
#             if robot_type not in groups:
#                 groups[robot_type] = []
#             groups[robot_type].append(robot)
#         return groups
#
#     def daily_report(self):
#         """Ежедневный отчёт (использует много разных методов)"""
#         report = []
#         report.append(f"\n=== Отчёт станции '{self.name}' ===")
#         report.append(f"Всего роботов: {len(self.robots)}")
#         report.append(f"Активных: {len(self.get_active_robots())}")
#         report.append(f"Неактивных: {len(self.get_inactive_robots())}")
#
#         # Группировка по типам
#         groups = self.group_by_type()
#         for robot_type, robots_list in groups.items():
#             report.append(f"\n{robot_type}: {len(robots_list)} шт.")
#             for robot in robots_list:
#                 report.append(f"  - {robot}")
#
#         # Общая статистика
#         report.append(f"\n{BaseRobot.get_total_count()}")
#
#         return "\n".join(report)
#
#     def emergency_shutdown(self):
#         """Аварийное отключение всех роботов"""
#         for robot in self.robots:
#             robot.is_active = False
#         self.logger.log("АВАРИЙНОЕ ОТКЛЮЧЕНИЕ! Все роботы деактивированы")
#
#     def __str__(self):
#         return f"Космическая станция '{self.name}' ({len(self.robots)} роботов)"
#
#
# main.py — Главная программа

# from robots import *
# from station import SpaceStation
# from logger import RobotLogger
#
#
# def main():
#     # Создаём космическую станцию
#     print("=" * 50)
#     print("ЗАПУСК СИСТЕМЫ КОСМИЧЕСКОЙ СТАНЦИИ")
#     print("=" * 50)
#
#     station = SpaceStation("Альфа Центавра")
#
#     # Создаём разных роботов
#     researcher1 = ResearchRobot("Кьюри", "RR-100", "биологическая")
#     researcher2 = ResearchRobot("Галилей", "RR-200", "физическая")
#
#     repair_bot = RepairRobot("Вулкан", "RP-500", ["паяльник", "отвёртка", "сканер"])
#
#     guard1 = SecurityRobot("Сторож", "SG-700", "лазер")
#     guard2 = SecurityRobot("Бульдог", "SG-800", "сеть")
#
#     # Универсальный робот с множественным наследованием
#     universal = UniversalRobot("Мульти", "UR-900", "химическая", ["анализатор", "манипулятор"])
#
#     # Добавляем роботов на станцию
#     for robot in [researcher1, researcher2, repair_bot, guard1, guard2, universal]:
#         station.add_robot(robot)
#
#     print("\n" + "=" * 50)
#     print("ТЕСТИРОВАНИЕ ВОЗМОЖНОСТЕЙ СИСТЕМЫ")
#     print("=" * 50)
#
#     # Тестируем исследователей
#     print("\n1. Исследовательская деятельность:")
#     for i in range(3):
#         print(f"  Попытка {i + 1}: {researcher1.conduct_research(f'Эксперимент {i + 1}')}")
#
#     # Тестируем охранников
#     print("\n2. Патрулирование:")
#     sectors = ["Сектор A", "Сектор B", "Сектор C"]
#     for sector in sectors:
#         print(f"  {sector}: {guard1.patrol(sector)}")
#
#     # Сравнение охранников (магический метод __gt__)
#     print(f"\n3. Сравнение охранников:")
#     print(f"  {guard1} > {guard2}? {guard1 > guard2}")
#     print(f"  Угроз нейтрализовано: {guard1.name}={guard1.threats_neutralized}, "
#           f"{guard2.name}={guard2.threats_neutralized}")
#
#     # Универсальный робот
#     print("\n4. Универсальный робот:")
#     print(f"  {universal}")
#     print(f"  Режим по умолчанию: {universal.mode}")
#     print(f"  Смена режима: {universal.switch_mode('ремонтник')}")
#
#     # Делаем одного робота неактивным для ремонта
#     researcher2.is_active = False
#     print(f"  {universal.perform_action(researcher2)}")
#
#     # Создаём группу роботов (композиция)
#     print("\n5. Группа роботов:")
#     group = guard1 + guard2  # Используем магический метод __add__
#     print(f"  Создана группа из {len(group)} роботов")
#     print(f"  Общая энергия группы: {group.total_energy()}")
#
#     # Ежедневный отчёт
#     print("\n" + "=" * 50)
#     print("ЕЖЕДНЕВНЫЙ ОТЧЁТ СТАНЦИИ")
#     print("=" * 50)
#     print(station.daily_report())
#
#     # Проверяем валидацию имени (статический метод)
#     print("\n6. Валидация имён:")
#     test_names = ["R2D2", "X", "123", "BB-8"]
#     for name in test_names:
#         is_valid = BaseRobot.validate_name(name)
#         print(f"  '{name}': {'валидно' if is_valid else 'невалидно'}")
#
#     # Читаем лог
#     print("\n" + "=" * 50)
#     print("ПОСЛЕДНИЕ ЗАПИСИ В ЛОГЕ")
#     print("=" * 50)
#     log_content = RobotLogger.read_log()
#     # Показываем последние 10 строк
#     last_lines = log_content.split('\n')[-10:]
#     for line in last_lines:
#         if line:  # Пропускаем пустые строки
#             print(f"  {line}")
#
#     print("\n" + "=" * 50)
#     print("СИСТЕМА УСПЕШНО ПРОТЕСТИРОВАНА")
#     print("=" * 50)
#
#
# if __name__ == "__main__":
#     main()






                            #⭐A-2. Структура нашего проекта
# В этом уроке ты применишь:
# классы
# наследование
# инкапсуляцию
# полиморфизм
# магические методы
# статические и классовые методы
# Это уже уровень Junior–Middle Developer.
# Проект будет простой, но полностью “как в реальности”.
#
# Мы создадим:
# 1) Product — Товар
# 2) FoodProduct и TechProduct — наследники товара
# 3) Store — Магазин
# 4) User — клиент
# 5) Admin — админ (наследник User)






                                #⭐B-1. Создаём класс Product
# class Product:
#     product_count = 0
#
#     def __init__(self, name, price):
#         self.name = name
#         self.__price = price       # инкапсуляция
#         Product.product_count += 1
#
#     def get_price(self):
#         return self.__price
#
#     def __str__(self):
#         return f"{self.name} — {self.__price} сом"



                            #⭐B-2. Наследование (полиморфизм)
# FoodProduct — еда
# class FoodProduct(Product):
#     def __init__(self, name, price, calories):
#         super().__init__(name, price)
#         self.calories = calories
#
#     def info(self):
#         print(f"{self.name}: {self.calories} калорий")
#
# TechProduct — техника
# class TechProduct(Product):
#     def __init__(self, name, price, warranty):
#         super().__init__(name, price)
#         self.warranty = warranty
#
#     def info(self):
#         print(f"{self.name}: гарантия {self.warranty} месяцев")
#



                        # ⭐C-1. Класс Store (магазин)
# class Store:
#     def __init__(self, name):
#         self.name = name
#         self.products = []
#
#     def add_product(self, product):
#         self.products.append(product)
#
#     def show_products(self):
#         for p in self.products:
#             print(p)   # работает __str__
#
#     def find_by_name(self, name):
#         for p in self.products:
#             if p.name.lower() == name.lower():
#                 return p
#         return None



                #⭐ C-2. Создаём класс User (клиент магазина)
# class User:
#     user_count = 0
#
#     def __init__(self, name):
#         self.name = name
#         User.user_count += 1
#
#     def __str__(self):
#         return f"Пользователь: {self.name}"
#
#     @classmethod
#     def get_count(cls):
#         return cls.user_count



                    #⭐ D-1. Admin — наследник User
# class Admin(User):
#     def remove_product(self, store, product_name):
#         store.products = [p for p in store.products if p.name != product_name]
#         print(f"Товар {product_name} удалён админом {self.name}")




                    # ⭐D-2. Используем всё это в мини проекте
# store = Store("AU Market")
#
# # Создаём товары
# bread = FoodProduct("Хлеб", 25, 300)
# milk = FoodProduct("Молоко", 55, 600)
# phone = TechProduct("Samsung A12", 9000, 12)
#
# # Добавляем товары в магазин
# store.add_product(bread)
# store.add_product(milk)
# store.add_product(phone)
#
# # Покупатель
# u1 = User("Файиздин")
#
# # Админ
# adm = Admin("Абдухалил")
#
# # Показываем товары
# store.show_products()
#
# # Ищем товар
# found = store.find_by_name("Хлеб")
# print("\nНайден:", found)
#
# # Админ удаляет товар
# adm.remove_product(store, "Молоко")
#
# # Товары после удаления
# print("\nПосле удаления:")
# store.show_products()



                            # ⭐ Что ты изучил в Уроке 4?
                            # ✔ наследование
                            # ✔ инкапсуляция
                            # ✔ полиморфизм
                            # ✔ магические методы
                            # ✔ классовые методы
                            # ✔ работу с несколькими классами в одном проекте
                            # ✔ взаимодействие объектов
                            # ✔ поведение как в настоящем приложении
                            # Это уже реальный мини-backend проект.

                        # Что мы применили в проекте:
                        # Классы и объекты — BaseRobot, ResearchRobot, SpaceStation
                        # Наследование — ResearchRobot → BaseRobot
                        # Инкапсуляция — _energy, __serial, геттеры
                        # Полиморфизм — group_by_type(), perform_action()
                        # Магические методы — __str__, __eq__, __add__, __gt__
                        # Статические методы — validate_name(), read_log()
                        # Классовые методы — get_total_count()
                        # Множественное наследование — UniversalRobot
                        # Композиция — RobotGroup
                        # Модульность — разделение на файлы


                        #🎯 ЗАДАНИЕ УРОКА 4 (ОБЯЗАТЕЛЬНО)

                            #Homework A-1:
# class Transport:
#     def __init__(self, name, speed):
#         self.name = name
#         self.__speed = speed
#
#     def get_speed(self):
#         return self.__speed
#
#     def move(self):
#         print("Moving Car")
#
# class Car(Transport):
#     def move(self):
#         print(f"Машина {self.name} едет по дороге со скоростью {self.get_speed()} км/ч")
#
# class Plane(Transport):
#     def move(self):
#         print(f"Самолёт {self.name} летит по воздуху со скоростью {self.get_speed()} км/ч")
#
#
#
# class Garage:
#     def __init__(self):
#         self.transports = []
#
#     def add_transport(self, transport):
#         self.transports.append(transport)
#         print(f'{transport.name} Добавлен гараж')
#
#     def show_all(self):
#         print("Все транспорты на гараже")
#         for t in self.transports:
#             print(f'{t.name}')
#
#
#     def find_by_name(self, name):
#         for t in self.transports:
#             if t.name == name:
#                 return t
#         return None
#
#
#
# garage = Garage()
#
# car1 = Car('Toyota', 150)
# car2 = Car('Tesla', 200)
# plane1 = Plane('B-2', 2000)
# garage.add_transport(car1)
# garage.add_transport(car2)
# garage.add_transport(plane1)
#
# garage.show_all()
#
# found = garage.find_by_name('Toyota')
# if found:
#     print(f'Найден транспорт: {found.name}')
#     found.move()
#
# for t in garage.transports:
#     t.move()




                                #Homework A-2:
# import json
# import os
# import time
# from functools import lru_cache
# from datetime import datetime
#
# # ======================
# # Логирование с ротацией
# # ======================
# LOG_FILE = "station_log.json"
# LOG_BACKUP_DIR = "log_backup"
#
# os.makedirs(LOG_BACKUP_DIR, exist_ok=True)
#
# class Logger:
#     LEVELS = ["INFO", "WARNING", "ERROR"]
#
#     @staticmethod
#     def log(message, level="INFO"):
#         if level not in Logger.LEVELS:
#             level = "INFO"
#         log_entry = {
#             "time": datetime.now().isoformat(),
#             "level": level,
#             "message": message
#         }
#         # Чтение существующего лога
#         logs = []
#         if os.path.exists(LOG_FILE):
#             with open(LOG_FILE, "r") as f:
#                 try:
#                     logs = json.load(f)
#                 except:
#                     logs = []
#
#         # Ротация если лог слишком большой
#         if len(logs) >= 100:
#             timestamp = datetime.now().strftime("%Y%m%d%H%M%S")
#             os.rename(LOG_FILE, f"{LOG_BACKUP_DIR}/station_log_{timestamp}.json")
#             logs = []
#
#         logs.append(log_entry)
#         with open(LOG_FILE, "w") as f:
#             json.dump(logs, f, indent=4)
#
#         print(f"[{level}] {message}")
#
#
# # ======================
# # Базовый робот
# # ======================
# class Robot:
#     def __init__(self, name, energy=100):
#         if not name.isalnum():
#             raise ValueError("Имя робота должно содержать только буквы и цифры")
#         self.name = name
#         self.energy = energy
#
#     def perform_action(self, cost=10):
#         if self.energy <= 0:
#             Logger.log(f"{self.name} не может выполнять действия, энергия кончилась", "WARNING")
#             return False
#         self.energy -= cost
#         Logger.log(f"{self.name} выполняет действие, осталось энергии: {self.energy}", "INFO")
#         return True
#
#     @property
#     def is_active(self):
#         return self.energy > 0
#
# # ======================
# # Специализированные роботы
# # ======================
# class TransportRobot(Robot):
#     def __init__(self, name, energy=100, capacity=50):
#         super().__init__(name, energy)
#         self.capacity = capacity
#
#     def transport(self, cargo):
#         if self.perform_action(cost=5):
#             Logger.log(f"{self.name} транспортирует {cargo} кг", "INFO")
#
#
# class MedicalRobot(Robot):
#     def diagnose(self, patient):
#         if self.perform_action(cost=5):
#             Logger.log(f"{self.name} диагностирует {patient}", "INFO")
#
#     def treat(self, patient):
#         if self.perform_action(cost=10):
#             Logger.log(f"{self.name} лечит {patient}", "INFO")
#
#
# # ======================
# # Станция роботов
# # ======================
# class RobotStation:
#     def __init__(self):
#         self.robots = []
#
#     def add_robot(self, robot):
#         self.robots.append(robot)
#         Logger.log(f"Добавлен робот {robot.name}", "INFO")
#
#     def remove_robot(self, name):
#         self.robots = [r for r in self.robots if r.name != name]
#         Logger.log(f"Удалён робот {name}", "INFO")
#
#     def report(self):
#         print("\n=== Отчёт по станции ===")
#         for r in self.robots:
#             print(f"{r.name} | Энергия: {r.energy} | Активен: {r.is_active}")
#         print("========================\n")
#
#     def simulate_crisis(self):
#         Logger.log("Симуляция кризисной ситуации", "WARNING")
#         for r in self.robots:
#             r.energy = max(0, r.energy - 20)
#
#     def optimize_energy(self):
#         Logger.log("Оптимизация энергии между роботами", "INFO")
#         total_energy = sum(r.energy for r in self.robots)
#         avg_energy = total_energy // len(self.robots) if self.robots else 0
#         for r in self.robots:
#             r.energy = avg_energy
#
#     @property
#     def efficiency(self):
#         if not self.robots:
#             return 0
#         active_count = sum(1 for r in self.robots if r.is_active)
#         return active_count / len(self.robots)
#
# def run_tests():
#     print("\n=== Тестирование ===")
#     t1 = TransportRobot("T1", capacity=100)
#     m1 = MedicalRobot("M1")
#
#     assert t1.capacity == 100
#     t1.transport(50)
#     m1.diagnose("Пациент")
#     m1.treat("Пациент")
#
#     try:
#         Robot("!!!")
#     except ValueError:
#         print("Тест имени робота пройден")
#
#     # Граничный случай энергии
#     r = Robot("LowEnergy", energy=0)
#     assert not r.perform_action()
#
#     # Эффективность станции
#     station = RobotStation()
#     station.add_robot(t1)
#     station.add_robot(m1)
#     assert station.efficiency == 1.0
#     station.simulate_crisis()
#     assert station.efficiency < 1.0
#
#     print("Все тесты пройдены!")
#
#
# # ======================
# # Запуск
# # ======================
# if __name__ == "__main__":
#     run_tests()



                                            #Homework B-1:
# class Contact:
#     def __init__(self, name, phone_number, id):
#         self.name = name
#         self.phone = phone_number
#         self.id = id
#
#     @staticmethod
#     def validate_phone_number(phone_number):
#         if len(phone_number) == 10:
#             return True
#         return False
#
#
# class ContactList:
#     all_contacts = []
#     last_id = 0
#
#     @classmethod
#     def add_contact(cls, name, phone_number):
#         if Contact.validate_phone_number(phone_number):
#             cls.last_id += 1
#             contact = Contact(name, phone_number, cls.last_id)
#             cls.all_contacts.append(contact)
#             print(f"Контакт '{name}' добавлен с id={contact.id}")
#         else:
#             raise ValueError(f"Невозможно добавить контакт '{name}': неверный номер")
#
#     @classmethod
#     def remove_contact(cls, contact_id):
#         for contact in cls.all_contacts:
#             if contact.id == contact_id:
#                 cls.all_contacts.remove(contact)
#                 print(f"Контакт с id={contact_id} удалён")
#                 return
#         print(f"Контакт с id={contact_id} не найден")
#
#
#
#
# print(ContactList.all_contacts)  # []
#
# ContactList.add_contact("Доналд Трамп", "0700100200")
# ContactList.add_contact("Илон Маск", "0500123456")
#
#
# print("\nСписок контактов после добавления:")
# for contact in ContactList.all_contacts:
#     print(contact.name, contact.phone, contact.id)
#
# print(ContactList.last_id)
#
# print('\n--------')
# ContactList.remove_contact(1)
# ContactList.remove_contact(7)
#
# print("\nСписок контактов после удаления:")
# for contact in ContactList.all_contacts:
#     print(contact.name, contact.phone, contact.id)
#
# print('\nЕсли что тут ошибка выдаёт, потому что тут цифра не содержит =! 10, если что этот коммент Алинур писал:')
# ContactList.add_contact("Cristiano Ronaldo", "55512384")





                                        #Homework B-2:
# class Product:
#     def __init__(self, name, price, category):
#         self.name = name
#         self.price = price
#         self.category = category
#
#     def info(self):
#         print(f'Товар: {self.name}\nКатегория: {self.category}\nЦена: {self.price} сом')
#
# class PhoneCase(Product):
#     def __init__(self, name, price, category, material):
#         super().__init__(name, price, category)
#         self.material = material
#
#     def info(self):
#         print(f'Чехол: {self.name}\nКатегория: {self.category}\nМатериал: {self.material}\nЦена: {self.price}')
#
#
# class PowerBank(Product):
#     def __init__(self, name, price, category, power):
#         super().__init__(name, price, category)
#         self.power = power
#
#     def info(self):
#         print(f'PowerBank: {self.name}\nКатегория: {self.category}\nКиловатт: {self.power}\nЦена: {self.price}')
#

# product1 = PhoneCase("Силиконовый чехол", 1200, "чехол", "силикон")
# product2 = PowerBank('Hoco23',2500,'powerbank',30000)
# product3 = Product('Airpods3', 2000, 'eairphone')
#
# product1.info()
# product2.info()
# product3.info()



                                                #Homework C-1:
# class Track:
#     def __init__(self, title, author, duration):
#         self.title = title
#         self.author = author
#         self.duration = duration
#
#     def show(self):
#         print(f'Название трека: {self.title}\nИсполнитель: {self.author}\nДлительность: {self.duration}')
#
#
# class Playlist:
#     track_list = []
#
#     def add_track(self, track):
#             self.track_list.append(track)
#
#
#     def remove_track(self, title):
#         for track in self.track_list:
#             if track.title == title:
#                 self.track_list.remove(track)
#                 print(f"Трек '{title}' удалён")
#                 return
#             print(f"Трек '{title}' не найден")
#
#
#     def show(self):
#         if not self.track_list:
#             print("Плейлист пуст")
#             return
#
#         print("Плейлист:")
#         print("=" * 30)
#         for track in self.track_list:
#             track.show()
#
#
# # Создание треков
# track1 = Track("Shape of You", "Ed Sheeran", "3:53")
# track2 = Track("Blinding Lights", "The Weeknd", "3:20")
# track3 = Track("Believer", "Imagine Dragons", "3:24")
#
# # Создание плейлиста
# playlist = Playlist()
#
# # Добавление треков в плейлист
# playlist.add_track(track1)
# playlist.add_track(track2)
# playlist.add_track(track3)
#
# # Вывод всех треков
# playlist.show()



                                                 #Homework C-2:
# class Client:
#     def __init__(self, name, balance):
#         self.name = name
#         self.balance = balance
#
#     def add_money(self, amount):
#         if amount > 0:
#             self.balance += amount
#             print(f"{self.name} пополнил счёт на {amount}")
#         else:
#             print("Сумма пополнения должна быть больше 0")
#
#
#     def spend_money(self, amount):
#         if amount <= 0:
#             print("Сумма списания должна быть больше 0")
#             return
#
#         if amount <= self.balance:
#             self.balance -= amount
#             print(f"{self.name} потратил {amount}")
#         else:
#             print("Недостаточно средств")
#
#
#     def info(self):
#         print(f"Name: {self.name}\nBalance: {self.balance}")
#
# class VIPClient(Client):
#     def __init__(self, name, balance, cashback):
#         super().__init__(name, balance)
#         self.cashback = cashback
#
#
#     def spend_money(self, amount):
#         if amount <= 0:
#             print("Сумма списания должна быть больше 0")
#             return
#
#         if amount <= self.balance:
#             self.balance -= amount
#             cashback_amount = amount * self.cashback / 100
#             self.balance += cashback_amount
#             print(f"{self.name} потратил {amount}")
#             print(f"Кэшбэк: {cashback_amount}")
#         else:
#             print("Недостаточно средств")
#
#
# client = Client("Иван", 1000)
# vip_client = VIPClient("Алексей", 1000, 5)
#
# client.info()
# vip_client.info()
#
# client.add_money(500)
# vip_client.add_money(500)
#
# client.spend_money(1000)
# vip_client.spend_money(1000)
#
# client.info()
# vip_client.info()



                                            #Homework C-3:
# class Clothes:
#     def __init__(self, size, price, type):
#         self.size = size
#         self.price = price
#         self.type = type
#
#     def describe(self):
#         print(f'Clothes size: {self.size}\nPrice: {self.price}\nType: {self.type}')
#
# class DiscountClothes(Clothes):
#     def __init__(self, size, price, type, discount_percent):
#         super().__init__(size, price, type)
#         self.discount_percent = discount_percent
#
#     def final_price(self):
#         discount_amount = self.price * self.discount_percent / 100
#         final_price = self.price - discount_amount
#         return final_price
#
# tshirt = Clothes("M", 2000, "Футболка")
# pants = Clothes("L", 3500, "Штаны")
# jacket = Clothes("XL", 5000, "Куртка")
#
# # Вывод информации о товарах
# tshirt.describe()
# pants.describe()
# jacket.describe()
#
# # Создание товаров со скидкой
# discount_tshirt = DiscountClothes("M", 2000, "Футболка", 20)
# discount_pants = DiscountClothes("L", 3500, "Штаны", 15)
# discount_jacket = DiscountClothes("XL", 5000, "Куртка", 10)
#
# # Вывод финальной цены со скидкой
# print(f"Финальная цена {discount_tshirt.type}: {discount_tshirt.final_price()} сом")
# print(f"Финальная цена {discount_pants.type}: {discount_pants.final_price()} сом")
# print(f"Финальная цена {discount_jacket.type}: {discount_jacket.final_price()} сом")





                                        #Homework C-4
# class Animal:
#     def __init__(self, name):
#         self.name = name
#
#     def sound(self):
#         pass
#
# class Dog(Animal):
#     def __init__(self, name):
#         Animal.__init__(self, name)
#
#     def sound(self):
#         print(f"{self.name}: woof, woof, woof")
#
# class Cat(Animal):
#     def __init__(self, name):
#         Animal.__init__(self, name)
#
#     def sound(self):
#         print(f"{self.name}: meow, meow, meow")
#
# class Cow(Animal):
#     def __init__(self, name):
#         Animal.__init__(self, name)
#
#     def sound(self):
#         print(f"{self.name}: myy, myy, myy")
#
# print('\nDOG:')
# rek = Dog('Rek')
# rek.sound()
#
# print('\nCAT:')
# eliza = Cat('Eliza')
# eliza.sound()
#
# print('\nCOW:')
# mariya = Cow('Mariya')
# mariya.sound()



                                    #Homework C-5:
# class BaseRobot:
#     def __init__(self, name, energy):
#         self.name = name
#         self.energy = energy
#
#     def use_energy(self, amount):
#         if self.energy >= amount:
#             self.energy -= amount
#             return True
#         return False
#
#     def __str__(self):
#         return f"Робот {self.name}, энергия: {self.energy}"
#
# class ChefRobot(BaseRobot):
#     def __init__(self, name, energy, specialty):
#         super().__init__(name, energy)
#         self.specialty = specialty
#         self.recipes_known = []
#         self.dishes_cooked = 0
#
#
#     def learn_recipe(self, recipe_name):
#         if recipe_name not in self.recipes_known:
#             self.recipes_known.append(recipe_name)
#             print(f"{self.name} выучил рецепт: {recipe_name}")
#         else:
#             print(f"{self.name} уже знает рецепт: {recipe_name}")
#
#     def cook_dish(self, recipe_name):
#         if recipe_name not in self.recipes_known:
#             print(f"{self.name} не знает рецепт '{recipe_name}'")
#             return
#
#         if self.use_energy(10):
#             self.dishes_cooked += 1
#             print(f"{self.name} приготовил блюдо '{recipe_name}'")
#         else:
#             print(f"{self.name} не хватает энергии для готовки")
#
#
#     def get_menu(self):
#         return self.recipes_known
#
#
#     def __str__(self):
#         return (
#             f"👨‍🍳 Робот-повар: {self.name}\n"
#             f"Специализация: {self.specialty}\n"
#             f"Энергия: {self.energy}\n"
#             f"Известные рецепты: {len(self.recipes_known)}\n"
#             f"Приготовлено блюд: {self.dishes_cooked}\n"
#         )
#
#     # ===== ТЕСТИРОВАНИЕ =====
#
# chef1 = ChefRobot("ChefBot-A", 50, "десерты")
# chef2 = ChefRobot("ChefBot-B", 30, "суши")
#
# chef1.learn_recipe("Тирамису")
# chef1.learn_recipe("Чизкейк")
# chef2.learn_recipe("Роллы")
# chef2.learn_recipe("Суши")
#
# chef1.cook_dish("Тирамису")
# chef1.cook_dish("Чизкейк")
#
# chef2.cook_dish("Суши")
# chef2.cook_dish("Пицца")  # неизвестный рецепт
#
# print("\nМеню ChefBot-A:", chef1.get_menu())
# print("Меню ChefBot-B:", chef2.get_menu())
#
# print("\n--- Информация о роботах ---")
# print(chef1)
# print(chef2)
#






                                            #Homework C-6:
# # ===== Базовый робот =====
# class BaseRobot:
#     def __init__(self, name, energy, is_active=True):
#         self.name = name
#         self.energy = energy
#         self.is_active = is_active
#
#     def __str__(self):
#         status = "активен" if self.is_active else "сломался"
#         return f"🤖 {self.name} | Энергия: {self.energy} | Статус: {status}"
#
# class RepairSystem:
#     def __init__(self):
#         self.__repair_queue = []
#
#
#     def add_to_queue(self, robot):
#         if not robot.is_active:
#             self.__repair_queue.append(robot)
#             print(f"{robot.name} добавлен в очередь на ремонт")
#         else:
#             print(f"{robot.name} активен — ремонт не нужен")
#
#
#     def repair_next(self):
#         if not self.__repair_queue:
#             print("Очередь ремонта пуста")
#             return
#
#         robot = self.__repair_queue.pop(0)
#         robot.is_active = True
#         robot.energy += robot.energy * 0.5
#         print(f"{robot.name} успешно отремонтирован")
#
#
#     def get_queue_length(self):
#         return len(self.__repair_queue)
#
#     def clear_queue(self):
#         self.__repair_queue.clear()
#         print("Очередь ремонта очищена")
#
#
#
# robot1 = BaseRobot("Alpha", 40, False)
# robot2 = BaseRobot("Beta", 60, False)
# robot3 = BaseRobot("Gamma", 80, True)
#
# repair_system = RepairSystem()
#
# repair_system.add_to_queue(robot1)
# repair_system.add_to_queue(robot2)
# repair_system.add_to_queue(robot3)  # активный, не добавится
#
# print("\nДлина очереди:", repair_system.get_queue_length())
#
# repair_system.repair_next()
# repair_system.repair_next()
# repair_system.repair_next()  # очередь пуста
#
# print("\nСостояние роботов после ремонта:")
# print(robot1)
# print(robot2)
# print(robot3)




                        #Homework C-7:
# class Car:
#     def move(self):
#         return "Еду по дороге"
#
# class Plane:
#     def move(self):
#         return "Лечу в небе"
#
# class Boat:
#     def move(self):
#         return "Плыву по воде"
#
#
# class TransformerRobot(Car, Plane, Boat):
#     def __init__(self):
#         self.current_form = "car"
#         self.history = []
#
#     def transform_to(self, form_name):
#         self.current_form = form_name
#         self.history.append(self.current_form)
#
#     def move(self):
#         if self.current_form == "car":
#             return Car.move(self)
#         elif self.current_form == "plane":
#             return Plane.move(self)
#         elif self.current_form == "boat":
#             return Boat.move(self)
#
#     def __call__(self):
#         print(f"Я сейчас в форме: {self.current_form}")
#
#     def __getitem__(self, index):
#         return self.history[index]
#
#     def __iter__(self):          # ✅ ВНУТРИ КЛАССА
#         return iter(self.history)
#
#
# robot = TransformerRobot()
#
# robot.transform_to("car")
# robot.transform_to("plane")
# robot.transform_to("boat")
#
# robot()              # __call__
# print(robot[0])      # __getitem__
#
# for form in robot:   # __iter__
#     print(form)
#
# print(robot.move())  # зависит от current_form




                                    #Homework C-8
# class Vehicles:
#     def start(self):
#         print('Vehicles starting')
#
#
# class Car(Vehicles):
#     def start(self):
#         super().start()
#         print('Car starting')
#
# class ElectricCar(Vehicles):
#     def start(self):
#         super().start()
#
#
#
# class Tesla(ElectricCar, Car):
#     def start(self):
#         super().start()
#         print('Tesla ready')

# print(Tesla.mro())
# tesla1 = Tesla()
# tesla1.start()




                                                #Homework C-9
# class BaseView:
#     def render(self):
#         print("Template render")
#
# class LoggingMixin:
#     def render(self):
#         print("Log: start")
#         super().render()
#         print("Log: end")
#
# class AuthRequiredMixin:
#     def __init__(self, authed=True):
#         self.authed = authed
#
#     def render(self):
#         if self.authed:
#             print("Auth OK")
#             super().render()
#         else:
#             print("Access denied")
#
#
# class AdminPageView(LoggingMixin, AuthRequiredMixin, BaseView):
#     def __init__(self, authed=True):
#         self.authed = authed
#         super().__init__(authed==authed)
#
#     def render(self):
#         print("Admin page render start")
#         super().render()
#         print("Admin page render end")
#
# print('\nПри авторизованном:')
# admin_page = AdminPageView(authed=True)
# admin_page.render()
#
# print('\nПри неавторизованном:')
# admin_page = AdminPageView(authed=False)
# admin_page.render()










#----------------------------------------------------------------------------------------------------------------
#----------------------------------------------------------------------------------------------------------------





               # 🧠 Урок 5 — Виртуальная среда, модули Python, свои модули, pip и Git
                #Урок 5: Организация проекта, Виртуальные среды и Контроль версий

# Объясняю, как для ребёнка:
# Представь, что ты художник. У тебя:
# Разные картины (проекты) — их нужно хранить отдельно, чтобы краски не смешивались (виртуальные среды)
# Краски из магазина (внешние библиотеки) — их можно купить и использовать (установка модулей)
# Свои авторские краски (собственные модули) — ты их сам создал
# Фотографии процесса (Git) — чтобы видеть, как менялась картина, и возвращаться к старым версиям

                            #A-1: Виртуальные среды (Virtual Environments)
# Зачем нужны? Чтобы изолировать зависимости разных проектов.
# Аналогия: У тебя два рецепта:
# Рецепт 1: нужен сахар
# Рецепт 2: сахар НЕ нужен
# Если положишь сахар на общую полку — он будет доступен для обоих рецептов.
# Виртуальная среда — это отдельная полка для каждого рецепта.
# Создание виртуальной среды:
# bash
# # В командной строке (терминале)
#
# # 1. Переходим в папку проекта
# cd C:/Users/ТвоеИмя/my_project
#
# # 2. Создаём виртуальную среду
# python -m venv venv  # Создаст папку 'venv' с изолированным Python
#
# # 3. Активируем виртуальную среду
# # Для Windows:
# venv\Scripts\activate
# # Для Mac/Linux:
# source venv/bin/activate
#
# # Теперь в начале строки появится (venv) - значит среда активирована
# (venv) C:/Users/ТвоеИмя/my_project>
#
# # 4. Деактивация среды
# deactivate
# Что внутри папки venv?
# text
# venv/
# ├── Scripts/      # Исполняемые файлы (activate, python.exe)
# ├── Lib/          # Установленные библиотеки (только для этого проекта)
# └── Include/      # Заголовочные файлы



                    #🔥A-2. Виртуальная среда (venv) — объяснение как ребёнку

# Представь, что Python — это кухня.
# А виртуальная среда — отдельная маленькая кухня, где лежат только те продукты, которые нужны для конкретного блюда.
#
# 👉 Почему это важно?
# Потому что:
# один проект может использовать библиотеку v1.0
# а другой — ту же библиотеку, но v5.0
# И они не должны мешать друг другу.
# Поэтому мы создаём маленькую личную кухню → виртуальную среду.
#
# ✔️ Как создать виртуальную среду в Windows
# Открой терминал/PowerShell:
# python -m venv venv
# Появится папка venv — это твоя маленькая кухня.
#
# ✔️ Как её включить:
# venv\Scripts\activate
# После активации ты увидишь:
# (venv)
# Это как индикатор — ты внутри своей маленькой кухни.
#
# ✔️ Как выключить:
# deactivate



                                            #B-1: Встроенные модули Python
# Python уже имеет много полезных модулей "из коробки". Это как инструменты в твоём рюкзаке.
# Примеры полезных встроенных модулей:
# # 1. Модуль os - работа с операционной системой
# import os
#
# # Узнать текущую папку
# print("Текущая папка:", os.getcwd())
#
# # Создать новую папку
# os.makedirs("my_folder", exist_ok=True)
#
# # Проверить, существует ли файл
# if os.path.exists("robots.py"):
#     print("Файл robots.py существует!")
#
# # Список файлов в папке
# files = os.listdir(".")
# print("Файлы в папке:", files)
#
# # 2. Модуль sys - работа с интерпретатором Python
# import sys
#
# # Пути, где Python ищет модули
# print("Пути поиска модулей:", sys.path)
#
# # Аргументы командной строки
# print("Аргументы:", sys.argv)
#
# # Версия Python
# print("Версия Python:", sys.version)
#
# # 3. Модуль math - математические функции
# import math
#
# print("Квадратный корень из 16:", math.sqrt(16))
# print("Число π:", math.pi)
# print("Синус 90 градусов:", math.sin(math.radians(90)))
#
# # 4. Модуль datetime - работа с датой и временем
# from datetime import datetime, timedelta
#
# now = datetime.now()
# print("Сейчас:", now)
# print("Год:", now.year)
# print("Месяц:", now.month)
# print("День:", now.day)
#
# # Завтра
# tomorrow = now + timedelta(days=1)
# print("Завтра:", tomorrow)
#
# # Форматирование даты
# print("Форматированная дата:", now.strftime("%d.%m.%Y %H:%M:%S"))
#
# # 5. Модуль random - случайные числа
# import random
#
# # Случайное число от 1 до 10
# print("Случайное число:", random.randint(1, 10))
#
# # Выбор случайного элемента из списка
# robots = ["R2-D2", "Валл-И", "Ева", "Оптимус"]
# print("Случайный робот:", random.choice(robots))
#
# # Перемешать список
# random.shuffle(robots)
# print("Перемешанный список:", robots)
#
# # 6. Модуль json - работа с JSON (очень важно для API!)
# import json
#
# # Данные Python → JSON строка
# robot_data = {
#     "name": "R2-D2",
#     "type": "астромеханик",
#     "height": 0.96,
#     "skills": ["пилотирование", "ремонт", "хакерство"]
# }
#
# json_string = json.dumps(robot_data, ensure_ascii=False, indent=2)
# print("JSON строка:")
# print(json_string)
#
# # JSON строка → данные Python
# parsed_data = json.loads(json_string)
# print("\nИмя робота из JSON:", parsed_data["name"])
#
# # Сохранение в файл
# with open("robot.json", "w", encoding="utf-8") as f:
#     json.dump(robot_data, f, ensure_ascii=False, indent=2)



                                #🔥B-2. Модули Python — как дитю объясняю
# Модуль — это файл с кодом, который можно использовать в другом файле.
#
# Например:
# Ты создал файл math_tools.py:
#
# def add(a, b):
#     return a + b
#
# def mul(a, b):
#     return a * b
#
# Теперь в другом файле можно подключить:
# import math_tools
#
# print(math_tools.add(2, 3))
#
# Это как отдельный инструмент:
# 📦 один раз написал — много раз используешь



                                            #C-1: Создание собственных модулей
# Модуль — это просто файл с расширением.py.Его можно импортировать в других файлах.
# Структура проекта:
# my_robots_project /
# ├── venv /  # Виртуальная среда
# ├── robots /  # Пакет (папка с модулями)
# │   ├── __init__.py  # Файл, делающий папку пакетом
# │   ├── base.py  # Базовые классы
# │   ├── specialized.py  # Специализированные роботы
# │   └── utils.py  # Вспомогательные функции
# ├── data /  # Данные
# │   └── robots.json
# ├── tests /  # Тесты
# │   └── test_robots.py
# ├── main.py  # Главный файл
# └── requirements.txt  # Зависимости
# Пример создания модуля:
# robots / base.py:
#
# """Базовый модуль для роботов"""
# class BaseRobot:
#     def __init__(self, name):
#         self.name = name
#
#     def introduce(self):
#         return f"Я робот {self.name}"
#
# def create_default_robot():
#     """Функция для создания робота по умолчанию"""
#     return BaseRobot("Безымянный")
#
# robots / specialized.py:
#
# """Модуль специализированных роботов"""
# from .base import BaseRobot  # Импорт из того же пакета
#
# class FlyingRobot(BaseRobot):
#     def fly(self):
#         return f"{self.name}: Лечу!"
#
# main.py:
#
# """Главный файл проекта"""
# # Разные способы импорта
#
# # 1. Импорт всего модуля
# import robots.base
#
# r1 = robots.base.BaseRobot("Робот1")
# print(r1.introduce())
#
# # 2. Импорт с псевдонимом
# import robots.specialized as sp
#
# r2 = sp.FlyingRobot("Робот2")
# print(r2.fly())
#
# # 3. Импорт конкретных классов
# from robots.base import BaseRobot, create_default_robot
#
# r3 = BaseRobot("Робот3")
# r4 = create_default_robot()
#
# # 4. Импорт всего (не рекомендуется!)
# # from robots.base import *
# robots / init.py:
#
# """
# Пакет robots.
# Здесь можно определить, что импортируется при from robots import *
# """
# from .base import BaseRobot
# from .specialized import FlyingRobot
#
# # Можно задать переменную __all__ для контроля импорта
# __all__ = ['BaseRobot', 'FlyingRobot']
#
# # Можно задать версию пакета
# __version__ = "1.0.0"




                                    #🔥C-2. Внешние модули (устанавливаются через pip)
# pip — это программа, которая устанавливает библиотеки из интернета.
# Например, хочешь установить библиотеку requests:
# pip install requests
#
# Потом ты можешь её использовать:
# import requests
#
# response = requests.get("https://google.com")
# print(response.status_code)



                                #D-1: Внешние модули и их установка
# Внешние модули устанавливаются из интернета. Самый популярный репозиторий — PyPI (Python Package Index).
# Установка модулей:
# bash
# # Активируем виртуальную среду
# venv\Scripts\activate
#
# # Устанавливаем модули
# pip install requests       # Для HTTP-запросов
# pip install numpy         # Для научных вычислений
# pip install pandas        # Для работы с таблицами
# pip install matplotlib    # Для графиков
# pip install flask         # Для веб-приложений
#
# # Установка конкретной версии
# pip install requests==2.28.1
#
# # Просмотр установленных пакетов
# pip list
#
# # Поиск пакета
# pip search "robot"
#
# # Удаление пакета
# pip uninstall requests
#
# # Сохранение зависимостей в файл
# pip freeze > requirements.txt
#
# # Установка из файла requirements.txt
# pip install -r requirements.txt
# Пример использования внешнего модуля:
# # Установи сначала: pip install requests
# import requests
#
# # Получение данных из интернета
# response = requests.get("https://api.github.com")
# print("Статус:", response.status_code)
# print("Заголовки:", response.headers['content-type'])
# print("Кодировка:", response.encoding)
#
# # Получение JSON данных
# data = response.json()
# print("Текущий пользователь GitHub:", data['current_user_url'])
#
# # Создание робота через API (пример)
# robot_data = {"name": "НовыйРобот", "type": "исследователь"}
# response = requests.post("https://api.example.com/robots", json=robot_data)
# print("Робот создан:", response.json())



                            #🔥 D-2. Использование собственных модулей (очень важно!)
# 📌 Правило:
# Модуль = файл .py
# Пакет = папка, где есть файл init.py
#
# Пример структуры:
# project/
#     venv/
#     utils/
#         __init__.py
#         helpers.py
#     main.py
#
# В main.py:
# from utils.helpers import greet
#
# greet("Fayizdin")




                                        #E-1: Основы Git — система контроля версий
# Git — это система, которая запоминает все изменения в коде. Как "машина времени" для твоего проекта.
#
# Основные команды Git:
# bash
# # 1. Инициализация репозитория
# git init  # Создаёт скрытую папку .git
#
# # 2. Проверка статуса
# git status  # Показывает изменённые файлы
#
# # 3. Добавление файлов в "промежуточную область"
# git add main.py           # Добавить конкретный файл
# git add .                 # Добавить ВСЕ файлы
# git add robots/*.py      # Добавить все .py файлы из папки robots
#
# # 4. Создание коммита (сохранение версии)
# git commit -m "Добавил базовых роботов"  # -m - сообщение
#
# # 5. Просмотр истории
# git log                  # Вся история
# git log --oneline       # Краткая история
# git log --graph         # История с ветками
#
# # 6. Отмена изменений
# git checkout -- file.py  # Отмена изменений в файле
# git reset HEAD file.py   # Убрать файл из промежуточной области
#
# # 7. Работа с ветками
# git branch              # Список веток
# git branch new-feature  # Создать ветку
# git checkout new-feature # Перейти в ветку
# git checkout -b new-feature # Создать и перейти
#
# # 8. Слияние веток
# git checkout main       # Перейти в main
# git merge new-feature   # Влить изменения из new-feature
#
# # 9. Удалённые репозитории (GitHub, GitLab)
# git remote add origin https://github.com/username/repo.git
# git push -u origin main  # Отправить изменения на GitHub
# git pull origin main     # Загрузить изменения с GitHub
# git clone https://github.com/username/repo.git  # Скачать репозиторий
# Типичный рабочий процесс:
# bash
# # День 1: Начинаем проект
# git init
# git add .
# git commit -m "Первоначальный коммит: базовая структура"
# git branch develop
# git checkout develop
#
# # День 2: Добавляем фичу
# # ... пишем код ...
# git add .
# git commit -m "Добавлен класс RobotFactory"
# git push origin develop
#
# # День 3: Исправляем баг
# # ... исправляем ошибку ...
# git add .
# git commit -m "Исправлена ошибка в методе calculate_power"
# git push origin develop
#
# # День 4: Вливаем в основную ветку
# git checkout main
# git merge develop
# git push origin main
# Файл .gitignore:
# Создай файл .gitignore в корне проекта, чтобы Git игнорировал ненужные файлы:
#
# # Виртуальные среды
# venv/
# .env/
#
# # Файлы Python
# __pycache__/
# *.pyc
# *.pyo
# *.pyd
#
# # IDE
# .vscode/
# .idea/
# *.swp
# *.swo
#
# # Операционные системы
# .DS_Store
# Thumbs.db
#
# # Логи и данные
# *.log
# *.sqlite3
# *.db
#
# # Временные файлы
# temp/
# tmp/



                                                #🔥F-1. Основы Git — как ребёнку
# Представь, что Git — это машина времени для твоего кода.
# Ты можешь:
# сохранить текущую версию (commit)
# вернуться назад, если что-то сломал
# отправить проект в интернет (GitHub)
#
# Самые важные команды:
# Создать репозиторий:
# git init
#
# Добавить файлы:
# git add .
#
# Сохранить:
# git commit -m "first commit"
#
# Посмотреть историю:
# git log
#
# Подключить GitHub:
# git remote add origin <URL>
# git push -u origin main



                                            #F-2: Полный пример проекта
                                        # Давай создадим полноценный проект снуля!
#
# Шаг 1: Создаём структуру проекта
# bash
# # В командной строке
# mkdir
# my_robots_project
# cd
# my_robots_project
#
# # Создаём виртуальную среду
# python - m
# venv
# venv
# venv\Scripts\activate  # Для Windows
#
# # Инициализируем Git
# git
# init
#
# # Создаём структуру папок
# mkdir
# robots
# data
# tests
# touch
# main.py
# README.md
# requirements.txt.gitignore
# touch
# robots / __init__.py
# robots / base.py
# robots / factory.py
# touch
# tests / test_base.py
#
# Шаг 2: Настраиваем.gitignore
# Создай файл.gitignore с содержимым из части 5 выше.
#
# Шаг 3: Пишем код
# robots / base.py:
#
# class BaseRobot:
#     def __init__(self, name, model):
#         self.name = name
#         self.model = model
#         self._energy = 100
#
#     def __str__(self):
#         return f"{self.name} ({self.model})"
#
#     @property
#     def energy_percentage(self):
#         return f"{self._energy}%"
#
#     def recharge(self):
#         self._energy = 100
#         return "Заряжен!"
#
# robots / factory.py:
#
# from .base import BaseRobot
# import random
#
# class RobotFactory:
#     models = ["T-100", "XJ-9", "Bender", "R2-D2"]
#     names = ["Арнольд", "Дженни", "Бендер", "Артуро"]
#
#     @classmethod
#     def create_random(cls):
#         name = random.choice(cls.names)
#         model = random.choice(cls.models)
#         return BaseRobot(name, model)
#
#     @staticmethod
#     def validate_robot(robot):
#         return hasattr(robot, 'name') and hasattr(robot, 'model')
#
# main.py:
# # !/usr/bin/env python3
# """
# Главный модуль проекта роботов
# """
# import json
# from robots.base import BaseRobot
# from robots.factory import RobotFactory
#
#
# def main():
#     print("=== Запуск фабрики роботов ===")
#
#     # Создаём роботов
#     robots = []
#     for i in range(3):
#         robot = RobotFactory.create_random()
#         robots.append(robot)
#         print(f"Создан: {robot}")
#
#     # Сохраняем в JSON
#     robots_data = []
#     for robot in robots:
#         robots_data.append({
#             "name": robot.name,
#             "model": robot.model,
#             "energy": robot.energy_percentage
#         })
#
#     with open("data/robots.json", "w", encoding="utf-8") as f:
#         json.dump(robots_data, f, indent=2, ensure_ascii=False)
#
#     print(f"\nСохранено {len(robots)} роботов в data/robots.json")
#
#     # Читаем из JSON
#     with open("data/robots.json", "r", encoding="utf-8") as f:
#         loaded_data = json.load(f)
#
#     print("\nЗагруженные роботы:")
#     for data in loaded_data:
#         print(f"  - {data['name']} ({data['model']})")
#
# if __name__ == "__main__":
#     main()
#
# Шаг 4: Создаём
# requirements.txt
# txt
# # requirements.txt
# # Основные зависимости
# requests >= 2.28
# .0
# # Для разработки
# pytest >= 7.0
# .0
# black >= 23.0
# .0  # Форматирование кода
# Шаг
# 5: Работа
# с
# Git
# bash
# # Добавляем файлы
# git
# add.
# git
# status  # Проверяем, что добавилось
#
# # Создаём первый коммит
# git
# commit - m
# "Initial commit: базовая структура проекта"
#
# # Создаём ветку для новой фичи
# git
# checkout - b
# add - flying - robot
#
# # ... работаем над фичей ...
#
# # Коммитим изменения
# git
# add.
# git
# commit - m
# "Добавлен класс FlyingRobot"
#
# # Возвращаемся в main и сливаем изменения
# git
# checkout
# main
# git
# merge
# add - flying - robot
#
# # Смотрим историю
# git
# log - -oneline - -graph
